<%= form_for(@paypal_account) do |f| %>
  <% if @paypal_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paypal_account.errors.count, "error") %> prohibited this paypal_account from being saved:</h2>

      <ul>
      <% @paypal_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
